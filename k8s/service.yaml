# Headlamp: 배포할 네임스페이스에서 적용.
apiVersion: v1
kind: Service
metadata:
  name: cicd-mcp
  labels:
    app.kubernetes.io/name: cicd-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cicd-mcp
  ports:
    - name: http
      port: 8765
      targetPort: http
