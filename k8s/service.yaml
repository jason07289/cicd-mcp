# Headlamp: 배포할 네임스페이스에서 적용.
apiVersion: v1
kind: Service
metadata:
  name: svn-mcp
  namespace: mcp
  labels:
    app.kubernetes.io/name: svn-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: svn-mcp
  ports:
    - name: http
      port: 80
      targetPort: http
